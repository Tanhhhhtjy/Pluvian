# 数据处理规范 (Data Handling Specification)

**项目**：npj AI4NWP — 雷达 + GNSS-PWV + 地面气象 + ERA5 多源短临降水预报
**版本**：v1（2026-05-24）
**状态**：基础数据已扫清，ERA5 下载中

---

## 1. 数据资产清单

```
/Data/tanh/npj/
├── radar_nc/        24144 .nc, 6min 一帧, 0.01°, 661×701, dBZ 整数反射率
│   ├── 202305/      24 天
│   ├── 202306/      16 天
│   ├── 202307/      25 天
│   ├── 202308/      22 天
│   ├── 202309/      14 天
│   ├── 20230728-雷达图/         478 PNG 参考图（colorbar=dBZ）
│   ├── 20230728-雷达图平铺/     同上 NaN→0 版本
│   └── README_说明.txt
├── pwv/             216 站 .txt, 30min, 16 列含 ZTD/ZHD/ZWD/PWV
├── stations/        389 站 .csv, 1h, 含 PRS/WIN_D/WIN_S/TEM/RHU/PRE_1h
│   └── README_物理量.txt
├── 2023_05-08_ERA5_PWV.nc       ERA5 tcwv 0.25° 1h（PWV 校验用）
└── era5/                         ERA5 压力层（下载中）
    └── era5_<var>_<level>_2023<MM>.nc   160 个文件
```

---

## 2. 雷达数据规范

### 2.1 物理量识别
- 变量名 `data0`，**实际为 dBZ 组合反射率 (Composite Reflectivity)**
- 文件存储为 int32 原始值 0–5300，带 `scale_factor=0.01` 属性 → **解码后**为 0.0–53.0 dBZ 浮点值
- **正确做法**：用 xarray / netCDF4 默认 `auto_scale=True` 读取（即使用解码后的 0–53 dBZ 浮点值）
- ⚠️ 早期文档误写"忽略 scale_factor 按整数读" → 已废止；如果忽略 scale_factor 会得到 5300 量级，Z-R 反演会爆 inf
- 已与 7-28 站点 PRE_1h 交叉验证（解码后 dBZ + Z=300·R^1.4 反演吻合）
- 推断为 CMA 多雷达拼图业务产品（SWAN/CRMS 量级）
- 单偏振（仅 ZH），无 ZDR/Kdp/CC

### 2.2 反射率→降水率反演
- 公式：**Z = 300 · R^1.4**（中国业务 Z-R）
- 实现：`R = (10**(dBZ/10) / 300) ** (1/1.4)` 单位 mm/h
- 论文里需明确：雷达 QPE 对强降水普遍低估 10–30%，作为已知偏差
- 弱降水 (dBZ<10) 因整数粒度信息有损失，可忽略<5dBZ 信号

### 2.3 时空规格
- 网格：lon [113.00, 120.00] × lat [36.00, 42.60]，0.01°，661×701
- 时间：6 分钟一帧，每天理论 240 帧，实际 235–240（缺帧 1–5 帧/天属正常）
- 缺帧处理：6min gap 用前一帧填充；连续>3 帧缺失则当日做 mask

### 2.4 缺日处理（依据"全网总降水"分类）
对 5-1 到 8-31 共 36 个雷达缺失日，按站点 PRE_1h 全网总和判定：

| 判定阈值 | 类别 | 天数 | 处理 |
|---|---|---|---|
| 全网总<50 mm 且 >0.1mm 站<50 | 真无雨 | 16 | 整日填零纳入训练（增加负样本） |
| 否则 | 雷达系统遗漏（仍有大量降水） | 20 | **丢弃**——loss 不计算，不当无雨 |

> ⚠️ README 写"无雨日没拷"，但实际 20 天有大量降水却雷达缺失。**不能盲目按缺失=无雨填零**。

### 2.5 9 月数据用法
- 9 月有雷达 14 天但无 PWV/站点
- **不进主训练**，专用作"PWV 缺失下的鲁棒性测试集"

---

## 3. PWV 数据规范

### 3.1 格式
- 16 列纯文本：`YYYY MM DD hh mm ss SITE LON LAT HGT PRES TEMP ZTD ZHD ZWD PWV`
- 时间步 30 分钟；时间范围 2023-02-01 → 2023-08-31
- 文件名中 "gps_era5_ztd_pwv_grad_tm" 是处理流程名，**实际无 grad/tm 字段**
- PRES/TEMP 为站点同址气象，**不要与气象站 PRS/TEM 混用，二者粒度和定义不同**

### 3.2 坐标精度问题
- PWV 坐标只到 **0.1°**（≈11 km）；气象站坐标到 **0.001°**（≈100 m）
- 138 个共享站码：用气象站精确坐标替换 PWV 坐标，**MFD 反演位置精度从 11 km → 100 m**
- 78 个 PWV 独有站码：保留 0.1° 精度

### 3.3 站点筛选规则
雷达框内（113–120°E, 36–42.6°N）共 183 站；按金重叠期（5-1→8-31）行数：

| 等级 | 阈值 | 站数 | 处理 |
|---|---|---|---|
| 🟢 优良 | ≥5500 行（>93%） | 154 | 全用 |
| 🟡 部分缺帧 | 5000–5500 行 | 14 | 用，单时刻缺则 mask |
| 🟠 显著缺失 | 1000–5000 行 | 8 | 保留，逐站验证关键事件期完整度 |
| 🔴 几乎无 | <1000 行 | 7 | **丢弃** |

**有效站数 = 176（154 + 14 + 8）**

需逐站验证的 8 个"显著缺失"站：
M0181_TJWQ、A2567_SW01、M0003_BJSH、53888_PINS、A1006_XCGY、53889_BULZ、53681_WUTA、53783_XIYA
（其中 BULZ 已验证 23·7 主事件 7-25~8-2 期间 100% 完整）

### 3.4 缺帧处理
- 每个 30min 时刻的每个站 → 有效/mask 二态
- 模型输入：(PWV_value, mask_indicator) 双通道
- 训练 batch 内要求站点参与率 ≥70%，否则换 batch

---

## 4. 地面气象站规范

### 4.1 格式
- 14 列 CSV：`Station Lat Lon Alti Year Mon Day Hour PRS WIN_D_INST WIN_S_INST TEM RHU PRE_1h`
- 时间步 1 小时；时间范围 2023-02-01 → 2023-08-31（9-1 仅边界点，等同 8-31 截止）
- 单位：PRS hPa, WIN_D 度, WIN_S m/s, TEM ℃, RHU %, PRE_1h mm

### 4.2 风字段说明
- 字段名 WIN_S_INST/WIN_D_INST 字面意思为"极大风速"，**但数据特征实际更接近小时平均风**
  - p50=2.4 m/s，p99=9.2 m/s（真阵风 p50 应在 4–7 m/s）
  - 相邻小时 |Δws| 中位数 0.7–0.8 m/s（真阵风应 2–3 m/s）
- 即便如此，站点风仅代表地表（10 m）层。**做 MFD 必须配 ERA5 850 hPa 高空风**

### 4.3 站点筛选
- 雷达框内 289 站
- 时间覆盖均良好（中位 4993 行 / 理论 ~5100）
- 全部纳入

---

## 5. ERA5 数据规范

### 5.1 已有
- `2023_05-08_ERA5_PWV.nc`：tcwv 单变量，0.25° 1h，5–8 月
- 用途：**PWV 一致性校验** + GNSS 缺帧时的背景场填补
- 不用于 MFD 或 CAPE

### 5.2 下载中（160 文件）
| 项 | 值 |
|---|---|
| 数据集 | reanalysis-era5-pressure-levels |
| 变量 | u_component_of_wind, v_component_of_wind, specific_humidity, temperature |
| 层 | 925, 850, 700, 600, 500, 400, 300, 250 hPa（8 层） |
| 时段 | 2023-05 → 2023-09，每小时 |
| 区域 | 110–122°E, 33–45°N |
| 分辨率 | 0.25°, 1h |
| 文件命名 | `era5_<var_short>_<level>_2023<MM>.nc`，如 `era5_u_850_202307.nc` |
| 预计总量 | ~640 MB |

### 5.3 ERA5 时空插值
- 时间：1h → 6min，**线性插值**（廓线类变量在 6min 内变化平缓）
- 空间：0.25° → 0.01°，**双线性插值**到雷达网格
- 边界：留出 1° 缓冲带，避免插值边缘效应
- ⚠️ **monthly time epoch 不统一**：派生量 (MFD/CAPE 等) 每月文件 `valid_time.units = "hours since 2023-MM-01 00:00:00"`，每月独立 epoch。跨月拼接时**必须用 cftime.num2date 解码**，不能 raw int64 直接 stack。

---

## 6. 派生量计算规范

### 6.1 水汽通量散度 MFD（核心物理通道）
- 公式：`MFD = -∇·(q·V) = -(∂(qu)/∂x + ∂(qv)/∂y)`
- 输入：ERA5 850 hPa 的 q, u, v（双线性插到 0.01°）
- 实现：`metpy.calc.divergence` 或手写 finite-difference + Gaussian smoothing (σ=2 格点)
- 层次：925, 850, 700 三层各算一遍，作为不同物理通道

### 6.2 不稳定指标
用 metpy 1.7+ 实现，对每个 (t, x, y) 网格点：

| 指标 | 函数 | 输入 |
|---|---|---|
| CAPE / CIN | `metpy.calc.cape_cin` | 8 层 p, T, Td, parcel_profile |
| Lifted Index | `metpy.calc.lifted_index` | 8 层 + 925 hPa parcel |
| K-index | `metpy.calc.k_index` | 850/700/500 T, Td |
| Total Totals | `metpy.calc.total_totals_index` | 850/500 T, Td |
| LCL | `metpy.calc.lcl` | 925 T, Td |
| Showalter | `metpy.calc.showalter_index` | 850→500 抬升 |

- q → Td 转换：`metpy.calc.dewpoint_from_specific_humidity`
- 性能：8 层全场 metpy 每小时约 3 秒，全季 ~60 万秒位置 × 5 月 ≈ 几小时离线预计算

### 6.3 CAPE 精度校验
- 用 ERA5 single-level 的 CAPE 产品作 ground truth（如需可补下）
- 8 层估算 vs 单层产品的相关系数预期 >0.85（探空对比文献量级）
- 若 <0.7 则补下 600/400/300 hPa 已含在 8 层中，无需再补

---

## 7. 时间分辨率对齐

**统一目标网格**：雷达 6 min × 0.01° × (113–120°E, 36–42.6°N)

| 源数据 | 原分辨率 | 时间对齐 | 空间对齐 |
|---|---|---|---|
| 雷达 | 6 min / 0.01° | 原生 | 原生 |
| PWV | 30 min / 站点 | 站点 token，时间 last-fill | cross-attention，不插值 |
| 地面站 | 1 h / 站点 | 站点 token，1h 内 hold | cross-attention，不插值 |
| ERA5 | 1 h / 0.25° | 6 min 线性插值 | 0.01° 双线性插值 |
| 派生 MFD/CAPE | 1 h / 0.25° | 同 ERA5 | 同 ERA5 |

---

## 8. 训练集划分

### 8.1 主训练池（103 天）
- 5-1 → 8-31 雷达存在的 87 天（含 23·7 主事件）
- + 16 个"真无雨日"补零纳入

### 8.2 验证集
- 23·7 主事件 (7-29 → 8-01, 4 天) **完全 hold out**
- 不参与训练、不参与超参选择

### 8.3 鲁棒性测试集
- 9 月雷达 14 天，PWV 通道清零，测"无 PWV 时模型退化幅度"

### 8.4 同期对比事件（用于多 case 评估）
- 2023-06-21~24（飑线）
- 2023-07-15~17（首轮季风性暴雨）
- 2023-08-09~12（卡努残涡）
- 2023-08-20~22（地形性强降水）

---

## 9. 数据隐私与开源约束

- **原始雷达 + PWV 数据不开源**（用户明确约束）
- **代码必须开源**（npj 要求）
- 论文 Data Availability 章节写：
  - 雷达：CMA SWAN/CRMS 业务产品，可向 CMA 公共气象服务中心申请
  - PWV：北斗/GPS 网络处理结果，相关单位申请
  - ERA5：完全开放，Copernicus CDS 链接给出
  - 站点：CMA 气象资料共享服务网申请

---

## 10. 已实现 vs 待实现

| 模块 | 状态 | 备注 |
|---|---|---|
| 数据扫描 + 完整性诊断 | ✅ | 见本规范 |
| ERA5 下载 | ⏳ 160 文件，2/160 | 后台运行 |
| 雷达 Z-R 反演 pipeline | 🔜 | 待 ERA5 完成后写 |
| PWV 站点筛选 + mask 生成 | 🔜 | 待写 |
| ERA5 时空插值到雷达网格 | 🔜 | 待写 |
| MFD 计算 | 🔜 | 待 ERA5 |
| CAPE/CIN/K-index 网格化计算 | 🔜 | 待 ERA5 |
| 数据集索引文件 (train/val/test split) | 🔜 | 待写 |
| 23·7 case study 物理诊断图 | 🔜 | 待全部数据齐 |
