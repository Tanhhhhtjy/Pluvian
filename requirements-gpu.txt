# Pluvian — GPU 训练专属依赖（A800 40GB, CUDA 12.1, Ubuntu 22.04）
#
# 安装方法:
#   pip install -r requirements-gpu.txt
#
# 注意:
#   1) 必须先装 base 依赖: pip install -r requirements.txt
#   2) 此文件指定了 PyTorch 的 cu121 wheel 源
#   3) flash-attn 编译耗时较长，必要时单独装: pip install flash-attn --no-build-isolation

--extra-index-url https://download.pytorch.org/whl/cu121

# ---- PyTorch (CUDA 12.1) ----
torch==2.2.0+cu121
torchvision==0.17.0+cu121
torchaudio==2.2.0+cu121

# ---- 训练 / 日志 ----
tensorboard>=2.16
einops>=0.7
timm>=1.0

# ---- 可选: 注意力加速 ----
# flash-attn>=2.5.0 ; sys_platform == "linux"

# ---- 监控 ----
nvidia-ml-py>=12.535

# ---- 数据并行 / 混合精度 ----
accelerate>=0.27
